#ifndef __TOOLS_H__
#define __TOOLS_H__
#include "type.h"
#include <string>
#include <vector>


using namespace std;

int qpow(int base, u32 k);

u32 pow2(u32 k);

u32 log2(u32 k);

u32 lowbit(u32 x);

#endif